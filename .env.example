# Kanboard Configuration
KANBOARD_URL=http://localhost:188/jsonrpc.php
KANBOARD_USER=jsonrpc
KANBOARD_TOKEN=

# OpenCode Configuration (Legacy)
# OPENCODE_CMD=opencode
# OPENCODE_MODEL=gpt-5.2-codex
OPENCODE_FALLBACK_MODEL=grok-code-fast
OPENROUTER_API_BASE=https://openrouter.ai/api/v1

# LLM Provider Configuration (Sprint 16-17)

# OpenRouter HTTP API (Recommended for production)
# Get key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=

# OpenCode CLI Provider (Optional)
# Command to invoke OpenCode CLI (default: opencode)
OPENCODE_CMD=opencode
# Subcommand for completions (default: run)
OPENCODE_SUBCOMMAND=run
# Model flag (default: --model)
OPENCODE_MODEL_FLAG=--model

# Gemini CLI Provider (Optional)
# Command to invoke Gemini CLI (default: gemini)
GEMINI_CMD=gemini
